name: 'Branch Checker'
description: 'Verfies if it is properly run semantic release in current branch'
outputs:
  match:
    description: 'it value is used to check whether versioning is applied or not'
runs:
  using: "composite"
  steps: 
    - name: check branch
      id: semantic-release-branch-checker
      shell: bash
      run: |
        if [[ ${GITHUB_REF##*/} =~ ^((+([0-9])?(.{+([0-9]),x}).x)|(master)|(beta))$ ]]; then
        echo ::set-output name=match::true
        fi
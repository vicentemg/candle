name: 'Semanti Release'
description: 'Greet someone'
runs:
  using: "composite"
  steps: 
    - name: semantic release 
      id: semantic-release
      shell: bash
      env: 
        GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
      run: echo $GITHUB_TOKEN $CI_COMMIT_BRANCH    
# - name: Semantic release
#         env:
#           GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
#         run: npx semantic-release